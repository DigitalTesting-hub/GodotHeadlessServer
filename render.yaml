services:
  - type: web
    name: godot-game-server
    env: docker
    docker:
      dockerfile: Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: 8080
    numInstances: 1
    healthCheckPath: /health
    autoDeploy: true
